<div class="container">
<h1>Вакансии</h1>

<ul> <% @vacancies.each do |vacancy| %>
  <li> <a href="/vacancies/<%= vacancy.id %>">
    <%= link_to vacancy.short_name, vacancy %>
    <div> <%= vacancy.body %> </div> </a>
  </li>
<% end %>
</ul>
</div>
